<view class="container">
    <van-tabs active="{{ active }}" bind:change="onChange">
        <block wx:for="{{tabs}}" wx:key="index" wx:for-item="tab">
            <van-tab title="{{tab.title}}">
                <scroll-view scroll-y refresher-enabled="{{true}}" refresher-threshold="{{100}}" refresher-default-style="white" refresher-background="{{refresherBackground}}" refresher-triggered="{{triggered}}" bindrefresherpulling="onPulling" bindrefresherrefresh="onRefresh" bindrefresherrestore="onRestore" bindrefresherabort="onAbort" class="content">
                    123232
                </scroll-view>
            </van-tab>

        </block>
    </van-tabs>
</view>